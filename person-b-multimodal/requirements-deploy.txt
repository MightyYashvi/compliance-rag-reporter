# Slim deploy deps — drops local Whisper/torch (whisper_mode defaults to "api")
# and langchain (never imported). Keeps the /generate + /api/* text path working.
# Image goes from ~2 GB to a few hundred MB, which fits free-tier hosts.

# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# LLM (SDKs used directly by app/services/generation.py)
anthropic==0.43.0
openai==1.58.1

# OCR (pytesseract needs the tesseract binary from the Dockerfile)
pytesseract==0.3.13
Pillow==11.1.0

# Document handling
python-docx==1.1.2
reportlab==4.2.5
jinja2==3.1.5

# Utilities
httpx==0.28.1
python-dotenv==1.0.1
aiofiles==24.1.0
uuid6==2024.7.10
